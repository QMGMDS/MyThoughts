# 思维导图
由于这部分的内容过多，纯文字和图片不能完全梳清楚知识关系
所以有疑惑或者细节的部分还请查看思维导图
![[Unity核心.xmind]]

# 认识模型制作的基本过程

美术-建模负责：
1. 建模：三角面片拼凑模型
2. 展UV：展开模型的网格到平面上产生映射关系
3. 材质和纹理贴图：使用UV信息画贴图选择材质出效果

美术-动作负责：
4. 骨骼绑定：对模型进行骨骼关联
5. 动画制作：利用模型骨骼制作动画

# 2D相关
## 图片导入相关设置
### 图片导入概述

- Unity 支持的相关图片素材：
	1. BMP：是 Windows 操作系统的标准图像文件格式，特点是几乎不进行压缩，占磁盘空间大
	2. TIF：基本不损失图片信息的图片格式，缺点是体积大
	3. JPG：一般指 JPEG 格式，属于有损压缩格式，能够让图像压缩在很小的存储空间，一定程度上会损失图片数据，无透明通道
	4. PNG：无损压缩算法的位图格式，压缩比高，生成文件小，有透明通道
	5. TGA：支持压缩，使用不失真的压缩算法，还支持编码压缩。体积小，效果清晰，兼备BMP的图像质量和JPG的体积优势，有透明通道
	6. PSD：是PhotoShop（PS）图形处理软件专用的格式，通过一些第三方工具或自制工具可以直接将PSD界面转为UI界面
	7. EXR
	8. GIF
	9. HDR
	10. IFF
	11. PICT
	12. 等等

点开一个图片素材，就可以在 Inspector 窗口中看见一堆参数设置
下面就是对这些参数设置的详细说明
这里只做出概述，保证提及能勾起印象
相关更详细的说明请查看思维导图

### 参数设置-纹理类型

![[Pasted image 20260411211814.png]]
`Texture Type` 即纹理类型，一共有八种纹理类型：
1. `Default` 默认纹理
2. `Normal map` 法线贴图模式
3. `Editor GUI and Legacy GUI` 编辑器 / GUI上使用的纹理
4. `Sprite (2D and UI)` 2D游戏 / UGUI中使用的纹理
5. `Cursor` 作为鼠标样式图的自定义光标
6. `Cookie` 光源剪影格式
7. `Lightmap` 光照贴图格式
8. `Single Channel` 纹理只需要单通道的格式
图片下方的参数会随着纹理类型的改变而发生改变
设置纹理类型主要是为了让纹理图片有不同的主要用途，哪种用途的图片应该去设置什么样的纹理类型，知道这个就足够了

### 参数设置-纹理形状

![[Pasted image 20260411215054.png]]
纹理不仅可以用于模型贴图，还可以用于制作天空盒和反射探针
设置纹理形状，主要就是用于在两种模式之间进行切换
设置为 2D 的纹理将使用到模型 / GUI元素上
设置为 Cube 的纹理将主要用于天空盒 / 反射探针上
注意：纹理类型设置为 `Default/Normal map/Single Channel` 才能对该参数进行修改

### 参数设置-高级设置

![[Pasted image 20260411220318.png]]
- 什么是 `MipMaps` ？
	在三维计算机图形的贴图渲染中有一个常用的技术被称为 Mipmapping
	该技术指为了加快渲染速度和减少图像锯齿，贴图被处理成由一系列被预先计算和优化过的图片组成的文件，这样的贴图文件被称为 `MipMap`
	`MipMap` 需要占用一定的内存空间
	
	`Mipmap` 中每一个层级的小图都是主图的一个特定比例的缩小细节的复制品
	虽然在某些必要的视角，主图仍然会被使用，来渲染完整的细节
	但是当贴图被缩小或者只需要从远距离观看时，`Mipmap` 就会转换到适当的层级
	
	因为 `Mipmap` 贴图需要被读取的像素远少于普通贴图，所以渲染的速度得到了提升，并且操作的时间减少了，因为 `Mipmap` 的图片已经是做过抗锯齿处理的，从而减少了实时渲染的负担，放大和缩小也因为 `Mipmap` 而变得更有效率
	
	如果贴图的基本尺寸是256x256像素的话,它 `Mipmap` 就会有8个层级
	每个层级是上一层级的四分之一的大小
	依次层级大小就是：128x128 64x64 32x32 16x16 8x8 4x4 2x2 1x1(一个像素)
	
	说人话：开启MipMap功能后，Unity会帮助我们根据图片信息生成n张不同分辨率的图片
	在场景中会根据我们离该模型的距离选择合适尺寸的图片用于渲染，提升渲染效率，以达到用内存换性能的目的

### 参数设置-平铺拉伸

![[Pasted image 20260411222050.png]]
平铺拉伸主要设置纹理的平铺规则以及拉伸规则
根据实际情况调整就可以了，哪种好看用哪种
这部分的参数暂时不用做太多了解

### 参数设置-平台设置

![[Pasted image 20260411222439.png]]
平台设置主要设置纹理最终打包时在不同平台的尺寸、格式、压缩方式
它非常重要，因为它影响了包大小和读取性能方面的问题

## Sprite

## 2D 物理系统

## Spriteshape

## Tilemap






































